node_modules/
dist/
build/
!apps/web/scripts/build/
!apps/web/scripts/build/*.mjs
coverage/
*.tsbuildinfo
# Every env file except the two committed templates. Written as a wildcard rather than a
# list because the list was already short by one: `.env.production` was not covered, and
# this repository is public.
.env
.env.*
!.env.example
!.env.production.example
.DS_Store
*.log
.pnpm-store/

# Local Vercel project links and pulled deployment metadata.
.vercel/

# Next.js build output and generated types. Committed once by accident when a .gitignore
# edit silently wrote to the wrong directory; 86 build artefacts went in with the web app.
.next/
next-env.d.ts

# Browser-level UI verification artefacts.
apps/web/test-results/
apps/web/playwright-report/
.vercel

# Machine-local Codex/plugin skill packages are not application dependencies.
.agents/
skills-lock.json
